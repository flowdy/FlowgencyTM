<h2>Administration dash</h2>

<h3>Mail loop</h3>

<ul>
% while ( my $user = $mailoop->next ) {
%    my $ml = $user->mailoop;
%    # if ( $ml->request_date is past and $ml->type eq 'invite') {
%    #    $user->delete;
%    #    next;
%    #}
<li><a href="/admin/view_user/<%= $user->user_id %>"><%= $user->username // $user->user_id %></a> (mail: <%= $user->email %>) requests <button><%= $ml->type %></button> <button>delete</button></li>
% }
</ul>
